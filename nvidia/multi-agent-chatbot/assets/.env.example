# N8N Configuration
# Set N8N_HOST to your domain (e.g., n8n.yourdomain.com) or localhost for local use
N8N_HOST=localhost

# Webhook URL - set to your public URL if using webhooks
WEBHOOK_URL=http://localhost:5678

# Encryption key for n8n credentials (auto-generated if left empty)
# Generate with: openssl rand -hex 32
N8N_ENCRYPTION_KEY=

# Timezone
TIMEZONE=UTC

# Docker volume directory (optional)
DOCKER_VOLUME_DIRECTORY=.
